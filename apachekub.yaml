apiVersion: apps/v1
kind: Deployment
metadata:
  name: apachekub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: proyectokubernetes
  template:
    metadata:
      labels:
        app: proyectokubernetes
    spec:
      containers:
      - name: apache
        image: joeleleazar/proyectokubernetes:latest
        volumeMounts:
        - name: data-vol
          mountPath: /var/www/html/data
        ports:
        - containerPort: 80